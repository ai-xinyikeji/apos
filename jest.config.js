const nextJest = require('next/jest')

const createJestConfig = nextJest({
  dir: './',
})

const customJestConfig = {
  setupFilesAfterEnv: ['<rootDir>/jest.setup.js'],
  testEnvironment: 'jest-environment-jsdom',
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/src/$1',
    // Prevent better-sqlite3 native addon from loading in tests
    '^better-sqlite3$': '<rootDir>/src/__mocks__/better-sqlite3.js',
    '^drizzle-orm/better-sqlite3$': '<rootDir>/src/__mocks__/drizzle-orm-sqlite.js',
    '^simple-git$': '<rootDir>/src/__mocks__/simple-git.js',
  },
  collectCoverageFrom: [
    'src/**/*.{js,jsx,ts,tsx}',
    '!src/**/*.d.ts',
    '!src/**/*.stories.{js,jsx,ts,tsx}',
    '!src/**/__tests__/**',
    '!src/**/__mocks__/**',
  ],
  coverageThreshold: {
    global: {
      branches: 30,
      functions: 30,
      lines: 30,
      statements: 30,
    },
  },
  testMatch: [
    '**/__tests__/**/*.[jt]s?(x)',
    '**/?(*.)+(spec|test).[jt]s?(x)',
  ],
  testPathIgnorePatterns: [
    '/node_modules/',
    '/.next/',
    '/data/',
    '/drizzle/',
  ],
}

module.exports = createJestConfig(customJestConfig)
